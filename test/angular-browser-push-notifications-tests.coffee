chai = require 'chai'
sinon = require 'sinon'

{Angularbrowser-push-notifications} = require '../src/angular-browser-push-notifications'

chai.should()

describe 'angular-browser-push-notifications', ->
  it 'should do something', ->
    true.should.equal true